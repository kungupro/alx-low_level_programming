#include <stdio.h>

/**
 * main - This includes the main function
 *
 * Return: This will retun to zero after after the code runs successfully
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
